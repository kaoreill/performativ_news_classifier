# Required: classifier provider
GROQ_API_KEY=your-groq-api-key-here

# Optional but recommended in deployment. The reader fallback used when a
# publisher blocks a direct fetch is rate-limited per source IP when
# unauthenticated, and a shared PaaS egress IP exhausts that limit quickly.
JINA_API_KEY=

PORT=8000
